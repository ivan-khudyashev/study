%top{
    #include "parser.hpp"
%}
%o fast dotall noyywrap bison-cc
%o namespace=yy
%o lexer=Lexer
%o lex=yylex
%o matcher=reflex
%%
[0-9]+              { 
                        std::cout << "lexer: number" << std::endl;
                        return yy::parser::token::CONST_NUMBER;
                    }
\"([^\\"]|\\\")*\"   {
                        std::cout << "lexer: string" << std::endl;
                        return yy::parser::token::CONST_STRING;
                    }
%%
